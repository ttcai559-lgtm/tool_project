# Scripts 目录说明

这个目录包含项目管理和维护脚本。

## 🔧 脚本列表

### force_restart.bat / force_restart.py
强制重启后端服务。

**用途：**
- 当后端服务无响应时使用
- 强制结束进程并重新启动

**使用方法：**
```bash
cd scripts
force_restart.bat
# 或
python force_restart.py
```

### restart_backend.bat
常规重启后端服务。

**用途：**
- 正常重启后端
- 会打开新的CMD窗口显示日志

**使用方法：**
```bash
cd scripts
restart_backend.bat
```

### kill_backend.py
结束后端进程。

**用途：**
- 单独停止后端服务
- 不重新启动

**使用方法：**
```bash
cd scripts
python kill_backend.py
```

## 💡 使用建议

### 日常使用
推荐使用根目录的启动脚本：
- `start_platform.bat` - 完整启动（显示窗口）
- `start_backend_hidden.vbs` - 静默启动后端
- `stop_platform.bat` - 停止服务

### 问题排查
当遇到服务问题时：
1. 使用 `kill_backend.py` 停止服务
2. 使用 `force_restart.py` 强制重启
3. 检查日志定位问题

---

**注意**: 这些是开发和调试用的脚本，生产环境建议使用系统服务。
